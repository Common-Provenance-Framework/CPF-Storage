replicaCount: 1

springProfilesActive: prod

config:
  NEO4J_URI: bolt://cpf-neo4j:7687
  STORE_URL: http://cpf-store:8080/api/v1/
  TRUSTED_PARTY_URL: http://cpf-trusted-party:8020/api/v1/
  CPF_STORE_VERBOSE_MODE: "false"

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 512Mi
